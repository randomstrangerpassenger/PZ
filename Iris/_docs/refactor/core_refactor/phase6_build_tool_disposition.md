# Phase 6 build-tool disposition

Status: `deferred_by_design`

No build-tool source, CLI, import path, serialization, or package writer was changed.

The governing closure at `Iris/_docs/round3/round3_active_core_closure.json` requires the current core closure to remain 12 modules and permits exactly four current-route regeneration tooling modules. All four slots are occupied:

1. `export_dvf_3_3_lua_bridge`
2. `iar_public_text_assessment`
3. `public_text_quality_acceptance`
4. `naturalization_compiler_identity`

The consolidated plan requires an explicit selection before a new current-route leaf can be imported: raise `max_allowed_modules`, replace an existing slot, create a non-current leaf, or defer. No residual defect was identified that would justify a slot-policy change, and the user-authorized plan does not approve raising or replacing the ceiling. Therefore the selected disposition remains `deferred_by_design` with current core `12` and allowed tooling `4/4` unchanged.

Re-entry requires a separate reviewed scope naming the exact module, owner class, caller, extraction reason, and slot disposition. Existing public entrypoints, historical paths, and the disposable package validator remain unchanged.
