# Affected Payload Rows

* current/current-looking forbidden row count: `0`
* predecessor rollback residue count: `2`

Predecessor residue rows:
* `Base.WeldingMask` / `rollback_snapshot_runtime` / `unadopted + exposed + non_nil text_ko`
* `farming.WateredCanFull` / `rollback_snapshot_runtime` / `unadopted + exposed + non_nil text_ko`
