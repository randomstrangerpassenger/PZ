"""Compatibility adapter for public-text composition.

Retained for existing build imports and dynamic callers.
"""
from __future__ import annotations

from iris_tooling.domains.public_text.composition.body_profile import (
    ADOPTED_RUNTIME_STATE,
    Any,
    CANDIDATE_STRUCTURAL_STATUSES,
    CANONICAL_RUNTIME_STATES,
    DEFAULT_LEGACY_COMPAT_LABEL_ERROR_CODE,
    DEFAULT_LEGACY_RUNTIME_STATE_ERROR_CODE,
    DEFAULT_RESOLVER_AUTHORITY_MODE,
    DIAGNOSTIC_RESOLVER_AUTHORITY_MODE,
    LEGACY_PROFILE_FALLBACK,
    LEGACY_RUNTIME_STATES,
    LEGACY_RUNTIME_STATE_ALIASES,
    Path,
    RESOLVER_AUTHORITY_MODES,
    SECTION_TO_PROPOSITION_ROLE,
    SELECTED_ROLE_TARGET,
    UNADOPTED_RUNTIME_STATE,
    UNKNOWN_RUNTIME_STATE_ERROR_CODE,
    build_body_plan_sections,
    build_candidate_body_plan_requirements,
    build_context_support_section,
    build_default_legacy_compat_label_error,
    build_single_proposition_equivalence_proof,
    canonical_proof_digest,
    derive_context_from_primary_use,
    derive_coverage_quality_candidate,
    ensure_sentence,
    has_text,
    hashlib,
    is_body_plan_profiles_v2,
    is_legacy_compat_profile_label,
    json,
    load_json,
    load_profile_resolution_rules,
    normalize_runtime_state,
    primary_use_covers_context,
    render_identity_core_text,
    resolve_body_profile,
    section_participates_in_minimums,
    select_limitation_source,
)
