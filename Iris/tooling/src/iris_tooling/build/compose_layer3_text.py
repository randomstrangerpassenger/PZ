"""Compatibility adapter for public-text composition.

Retained for existing build imports and dynamic callers.
"""
from __future__ import annotations

from iris_tooling.domains.public_text.composition.entrypoint import (
    Any,
    BODY_PLAN_PROFILES_PATH,
    BODY_SOURCE_OVERLAY_PATH,
    BUILD_TMP_ROOT,
    CLEAN_CHECKOUT_TEST_OUTPUT_ROOT_ENV,
    CLOSED_CURRENT_PROTECTED_PATHS,
    COMPOSE_CONTEXTS,
    COMPOSE_CONTEXT_OUTPUT_CLASS_ERROR_CODE,
    COMPOSE_CONTEXT_REQUIRED_ERROR_CODE,
    COMPOSE_CURRENT_UNLISTED_OUTPUT_ERROR_CODE,
    COMPOSE_EXTERNAL_OUTPUT_REQUIRED_ERROR_CODE,
    COMPOSE_INVALID_CONTEXT_ERROR_CODE,
    COMPOSE_PROFILE_CLASS_ERROR_CODE,
    CURRENT_AUTHORITY_INPUT_KEYS,
    CURRENT_COMPOSE_CONTEXT,
    CURRENT_OVERLAY_SUPPORT_PATH,
    CURRENT_PROTECTED_BASENAMES,
    ComposeEntrypointGuardError,
    Counter,
    DATA_DIR,
    DEFAULT_CURRENT_AUTHORITY_INPUT_PATH_ERROR_CODE,
    DEFAULT_MODE,
    DEFAULT_RESOLVER_AUTHORITY_MODE,
    DIAGNOSTIC_COMPOSE_CONTEXT,
    DIAGNOSTIC_RESOLVER_AUTHORITY_MODE,
    DIAGNOSTIC_RESOLVER_MODE,
    EDPAS_DIAGNOSTIC_DIR,
    ENTRYPOINT_MODES,
    HISTORICAL_COMPOSE_CONTEXT,
    IDENTITY_RULES_PATH,
    OUTPUT_DIR,
    OVERLAY_PATH,
    PRECEDENCE_RULES_PATH,
    Path,
    REAL_CLOSED_CURRENT_PROTECTED_PATHS,
    REGISTRY_FIXTURE_DECISION_SCHEMA,
    REGISTRY_FIXTURE_RECEIPT_INVALID_ERROR_CODE,
    REGISTRY_FIXTURE_RECEIPT_REPLAY_ERROR_CODE,
    REGISTRY_FIXTURE_RECEIPT_SCHEMA,
    REGISTRY_REAL_CURRENT_WRITE_DISABLED_ERROR_CODE,
    REPOSITORY_ROOT,
    ROOT,
    STAGING_COMPOSE_CONTEXT,
    STAGING_DIR,
    STYLE_LOG_PATH,
    TEST_TMP_ROOT,
    UNADOPTED_RUNTIME_STATE,
    VNEXT_EXECUTION_DIR,
    argparse,
    build_candidate_rendered,
    build_rendered,
    canonical_sha256,
    classify_compose_profile,
    classify_compose_write_path,
    compose_all_candidate,
    compose_all_legacy,
    compose_all_v2,
    compose_context_for_entrypoint,
    contains_legacy_sentence_plan,
    datetime,
    default_entrypoint_paths,
    enforce_allowed_output_classes,
    enforce_compose_write_contract,
    enforce_current_authority_input_contract,
    enforce_current_output_known_set,
    enforce_entrypoint_mode_contract,
    enforce_external_entrypoint_outputs,
    enforce_resolver_authority_output_contract,
    entries_sha256,
    errno,
    file_sha256,
    has_test_tmp_segment,
    hashlib,
    is_body_plan_profiles_v2,
    is_current_equivalent_name,
    is_known_current_protected_path,
    is_real_current_protected_path,
    is_under_path,
    is_vnext_staging_default_execution,
    json,
    load_json,
    load_jsonl,
    load_optional_jsonl_map,
    load_profile_resolution_rules,
    main,
    normalized_body_plan_authority,
    os,
    parse_args,
    re,
    read_registry_fixture_receipt,
    receipt_fixture_root,
    receipt_output_paths,
    registry_fixture_receipt_binding,
    require_compose_context,
    require_repository_context,
    resolve_entrypoint_paths,
    resolved,
    resolver_authority_mode_for_entrypoint,
    sha256_path,
    sys,
    tempfile,
    text_mode_write_bytes,
    time,
    timezone,
    validate_registry_fixture_receipt,
    write_json_retry,
    write_jsonl,
    write_lines_retry,
)


def build_shared_successor(repository_root: Path, output_path: Path) -> dict[str, Any]:
    """Produce a reviewable successor without adopting it or switching runtime."""
    from .compose_layer3_shared import build_shared_candidate
    return build_shared_candidate(repository_root, output_path)


if __name__ == "__main__":
    raise SystemExit(main())
