# Iris Layer 3 acquisition results — closeout

2026-09-04 · DVF-L3-04 · complete / adopted — G1 and actual adopted connection exit 0.

Implemented the adopted execution plan in
`iris_dvf_layer3_acquisition_evidence_independent_results_plan.md` under the
owner's execution, adoption and P4 recovery preapproval. This is an off-live
acquisition result authority and structured L3-05 handoff.

## Exact subject

- Manifest: `Iris/_docs/authority/dvf/layer3_acquisition_results/manifest.json`
- Manifest SHA-256: `0281e7db661d2c37984568b715e53c97a3e78234b95b9fdfbb59ea1e31fa2a29`
- Corpus SHA-256: `4293d715305978f4fec5b6643501cf73884e58dd4aec3e8692928b8880502512`
- Target set: 2,105 case-sensitive FullTypes; SHA-256 `122ca07c483ff8e4af9ef83bfb8d28c950802a124aba1668c234bce3477b2fdb`.
- Definition: L3-02 revision `1`, manifest SHA-256 `47be8947a0b18745560b1e7e2463adbe86ab878e5e9fefd461f2a838c164290e`.
- Independent L3-03 manifest: `a3416672aa47fe4c6c84d9b8e9912377adda6e20e9eb679bf2d229cb9d3456bd`.
- Inventory revision `1`: 339 distinct source members, six families, 12,630 required pairs and 3,351 assessed interpretation traces. Shared members and observations are reused across families.

Corpus lifecycle bytes remain candidate. The exact successful subject, current
route and adopted loader own adoption; no sealed predecessor was rehashed.

## Investigation and resulting facts

| Family | Members | Actual source → consumer interpretation and boundary |
|---|---:|---|
| loot | 4 | Procedural item lists → room/container procList references and ordinary distributions → ItemPickerJava. Weights, namespaces and loader precedence do not establish an exact spawn or probability. |
| vehicle | 89 | Vehicle item lists → container definitions → recursive distribution aliases and vehicle selection references, plus vehicle scripts. Engine selection/namespace and load precedence remain open. |
| foraging | 23 | Static definitions and ten literal generator loops → defaults/import/category eligibility → loot table → icon item list/callbacks → valid pickup action → inventory/ground. Item/category/zone/month/perk/recipe/trait/tool and action predicates are retained. |
| fishing_trapping | 25 | Registered fish/trash definitions with individual and later common lures → valid fishing/selection/line checks → exact creation and inventory. Animals → trap/bait/zone/hour/freshness/random capture → checked animal recovery. |
| dynamic | 160 | Actual creation/return/preview/stash/message callsites → enclosing function, assignments/guards and callers → source-specific interpretation. Confirmed starting inventory and broad recovery/incidental paths become facts; symbolic types, debug/test injection and unobserved engine message reception remain distinct. |
| transformation | 313 | Script result/replacement/callback clauses → exact callback source lookup and active action/RecipeManager boundary. Returned objects and confirmed exact replacements are distinguished from unqualified recipe outputs and engine-only transformations. No Layer 4 action catalogue is copied. |

All required pair investigations were performed. Literal queries are only
item-local leads within the shared semantic investigation; a hit/miss cannot
substitute for that work. The validator rejects unreviewed trace obligations,
unassigned members, omitted pairs, unknown outcomes and performed-state claims
over a missing attempt. The scope is this repository snapshot, not every engine
or mod path in the game.

| Result | Count |
|---|---:|
| acquisition resolved | 1,025 |
| investigated_unresolved | 1,080 |
| not_investigated | 0 |
| accepted positive facts | 1,057 |
| accepted negative facts | 0 |
| items with multiple accepted paths/conditions | 19 |
| facts with multiple provenance occurrences | 1 |
| combined item complete | 0 |

Facts comprise 1,007 foraging/crop-seed paths, 10 fishing, 5 trapping, 21
new-player conditional paths and 14 broad recovery/incidental/replacement paths.
No closed item-global negative is justified by this snapshot. The G1 synthetic
closed-negative case is only a fixture; it supplies no game fact or new authority.

Overlapping manual semantic samples and G1 checks:

- `Base.Worm` retains foraging, plowing and ground-digging paths. Foraging fact `fact:d1a17724769b6681f75ac00931cc32cd1242a4307cdf73c43e96439883897953`, plowing `fact:aa271b4b3725e44e5407c1c4ede55932006c7c4ad5a675fce072661af8d96c81`, digging `fact:8b5056bb7f97c39eb8144f356bd72d7b27d6520ed880dced2c154ff9ae7d8011`. Random branches and distinct prerequisites remain fact-local.
- `Base.Generator`, `fact:9149992937b6e8d4099136620104c628f82f7246ee3d139e7945a7914fe7f06f`: menu caller and valid existing, disconnected world generator → completed pickup → exact inventory item, preserving condition and positive fuel.
- `Base.Pike`, `fact:64e15c48480bc4a30bf5e7cdc5b50fb717c47ff0533882253319e6e084ffdd30`: property/lure registration and fishing consumer; later common-lure additions are included. `Base.DeadRabbit`, `fact:ea07ef867abe0de4f685d36aff20f1be774aece2601e416e342711a5c2bb94b5`: trap, bait, zone, time and capture/recovery conditions.
- `Base.BookTrapping1`: distribution/place/PostalTruckBed leads and actual KO/EN predecessor prose exist, but no accepted exact acquisition path. It remains investigated_unresolved with zero facts. Both empty acquisition role-ref arrays and the actual seven-place KO/EN prose are retained.
- Engine parts: the local AddItems block in `ISTakeEngineParts.lua` is commented out. Active client command → `VehicleCommands.Commands.takeEngineParts` → positive-count exact `addItemOfType` request is traced with both source locators. The absent engine message receiver is an explicit boundary, not invented local delivery.
- `Base.Bag_PistolCase`, `Base.Lemongrass`, `Base.NoiseMaker` have zero exact declarations; `Base.ShotgunCase1` has two. All retain their exact target identity, zero accepted acquisition facts and investigated_unresolved. No alias or arbitrary loader winner is used.
- A confirmed conditional path plus an unrelated uncertain additional path is resolved. The G1 unperformed fixture retains its independent confirmed fact as a partial contribution while remaining not_investigated. Acquisition resolution alone leaves the item incomplete.

## Predecessor disposition and preservation

The reconstructed material set M has 8,278 occurrences: 1,105 hints, 68
acquisition source-slot records, 4,210 product acquisition-role records and
2,895 expression surfaces. All are lead-only; there are zero nonempty absence
records, zero rebound/rejected records and no transfer of predecessor prose into
accepted truth. H=1,105; Book subset B=55; intersection=55; H-only=1,050;
B-only=0; union=1,105. The additional 13 non-Book source slots remain in M.

Actual locale surfaces are EN core/menu and KO core/menu/text_ko. The census
conservatively retains all expression surfaces, including non-acquisition text;
recognized acquisition claims retain their exact spans. Seven-place claim sets
agree for the actual paired Menu expressions; locale mismatch count is zero.
Empty typed role refs do not suppress prose discovery.

G1 compared all 330 initially enumerated protected paths and existing shared
registration projections. L3-01/02/03 members, L3-03 4,233 facts / 9,982 results,
partial bindings, non-acquisition resolver projection and scope/pending/gap are
preserved. The four identity anomalies remain explicit. Preservation claims are
limited to the enumerated paths and actual comparisons; no external installation
tree was inspected. Runtime/product, composer, KO/EN Menu/Tooltip, current
generation/pointer and package/install writers were not changed.

## G1 and adoption

One required identity:
`test_layer3_acquisition_results.Layer3AcquisitionResultsTest.test_acquisition_results_contract`.

PowerShell setup, all paths inside the repository:

```powershell
$env:PYTHONPATH = (Join-Path (Get-Location) 'Iris/tooling/src')
$env:PYTHONDONTWRITEBYTECODE = '1'
$env:UV_CACHE_DIR = (Join-Path (Get-Location) '.tmp/acquisition/cache')
$env:IRIS_REPOSITORY_ROOT = (Get-Location).Path
$env:IRIS_LAYER3_ACQUISITION_MODE = 'candidate'
uv run --project .\Iris\tooling --no-sync python -m pytest .\Iris\build\description\v2\tests\test_layer3_acquisition_results.py -q -p no:cacheprovider --round3-contract=diagnostic --round3-additional-source=Iris/build/description/v2/tests/test_layer3_acquisition_results.py
```

G1_EXIT_CODE=0 — `1 passed, 14 subtests passed in 9.27s`.
V1–V11 run together in this identity. The first final execution exited 1 in
5.85s because a test expected the word `receiver` while the actual trace said
`engine code absent`; the semantic boundary was already recorded. Corrected that
assertion, rebound the changed test member and reran G1 successfully. No interim
tests were run. Source production was monitored. Full canonical candidate
production ran once; a pre-G1 Book-subset census correction reused its existing source
evidence without a second survey. Temporary helpers/results remain execution aids.

The current registry is `Iris/validation/execution/required_validations.json`;
the active source policy remains
`Iris/_docs/round3/round3_pytest_source_classification.json`. Add exactly one
required identity, one current source row (53 tracked policy sources), one
authority entry and one route entry. Existing entries and historical registry
are preserved. Reader ownership is described in the acquisition contract.

Limited adopted connection uses the exact command above with
`$env:IRIS_LAYER3_ACQUISITION_MODE = 'adopted'`. It loads both actual adopted
readpoints and resolves 2,105 applications, checking exact subject/route/authority/
required membership and protected shared state. It does not rerun source
production, semantic samples or the fixture suite.

ADOPTED_CONNECTION_EXIT_CODE=0 — `1 passed in 5.32s`.
The actual adopted loaders and combined resolver consumed the exact verified
subject successfully. No further confidence tests were run after this connection.

L3-05 consumes `acquisition_consumption.load(root, manifest_binding, mode='adopted')`
for both corpora/readpoints, confirmed facts, conditions, provenance, open results
and resolver applications. Expression and omission handling remain L3-05;
runtime/product adoption remains L3-06. Item complete stays zero. Under the
preapproved P4 policy, a defective L3-04 route is withdrawn, predecessor sealed
history preserved and a new correction subject verified before readoption.
