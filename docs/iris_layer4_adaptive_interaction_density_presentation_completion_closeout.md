# Iris Layer 4 adaptive interaction density presentation completion closeout

Date: 2026-08-21

State: `implemented_only`

The previously deferred Layer 4 projection and adaptive Browser implementation
are now installed. Current QG positive rows are the only presentation source;
Recipe and Right-click are peer Sources; `0 / 1 / 2–8 / 9+` density behavior,
lossless full access, literal search, stable Recipe navigation, row-local
requirements, and generation/locale/item state invalidation are implemented.

The complete runtime projection validates at 1,631 FullTypes, 877 positive
rows, 791 Recipe rows, 321 unique Recipe identities, and 13 generated files.
V1, V2, V3, V4, focused adaptive/Gate 5 tests, and V6 disposable package/live
parity pass. The final exact V3 result is 471 passed, one adopted skip, 190
deselected, and 124 subtests passed.

Completion is not claimed. The historical assistant V5 run launched the actual
PZ executable but timed out after 240 seconds and produced no evidence rows, so
it receives no PASS credit. Further V5 execution and the 12-case in-game manual
matrix are handed to the repository owner and remain
`unvalidated_but_in_scope`; the assistant will not relaunch PZ. V7 is
`stale_due_to_layer4_change`, not a refreshed IPS result. Independent review
and the final owner canonical seal are pending, so canonical top documents
remain unchanged.

No package publication, release, deployment, performance, all-locale, or
all-resolution claim is made. Detailed implementation and validation bounds
are recorded in
`docs/iris_layer4_adaptive_interaction_density_presentation_completion_walkthrough.md`.
