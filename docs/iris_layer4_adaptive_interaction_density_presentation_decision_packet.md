# Iris Layer 4 adaptive interaction presentation decision packet

> Historical/ineligible packet notice (2026-08-21): this packet inferred owner approval that the user did not explicitly provide. It is retained only as part of the historical Gate 3 blocked record and is not eligible evidence for `L4-RAT-01`–`L4-RAT-07`, QG-only publication, an implementation subject, or a new owner seal. `L4-RAT-08` is now `not_applicable` because the fresh `recipe_only` set is empty. A new packet may be created only after the user supplies the required explicit choices.

> Status: STAGING DRAFT / owner-policy approval recorded / Gate 3 not passed
>
> Approval source: the repository owner's 2026-08-21 execution prompt pre-approves plan/document owner gates. This packet binds that approval to the exact policy below. It is not a canonical `DECISIONS.md` entry and does not authorize a migration-gate bypass.

## Approved presentation policy

| ID | Approved contract |
|---|---|
| `L4-RAT-01` | Four deterministic states: `0`, `1`, `2~8`, `9+` (`small_max=8`, `dense_min=9`). |
| `L4-RAT-02` | Browser owns a private runtime presentation projection; no new semantic or sealed policy artifact is introduced. |
| `L4-RAT-03` | Grouping uses Source only. Recipe and Right-click are peer sections. |
| `L4-RAT-04` | Presentation order is Recipe then Right-click, without priority meaning. |
| `L4-RAT-05` | QG line order and stable identity-set completeness are preserved. |
| `L4-RAT-06` | Dense items start compact, expose literal local search and use the existing single outer scroll. Single/small requirements are immediate; dense requirements may fold per row. |
| `L4-RAT-07` | Base order is locale-independent; Tooltip is regression-only; disposable package projection belongs to the full implementation path. |

The execution prompt did not select one of the three mutually exclusive `L4-RAT-08` branches by name. To avoid inventing a QG fact, deleting a producer fact, or broadening authority while the owner is unavailable, this packet binds the conservative no-mutation disposition `preserve_legacy` to every fresh planning-matched `qg_decided_no` tuple:

| FullType | recipe_id | Failure category | Disposition | Gate effect |
|---|---|---|---|---|
| `Base.HandTorch` | `remove_battery` | `qg_decided_no` | `preserve_legacy` | `remains_blocked` |
| `Base.Rubberducky2` | `remove_battery` | `qg_decided_no` | `preserve_legacy` | `remains_blocked` |
| `Base.Torch` | `remove_battery` | `qg_decided_no` | `preserve_legacy` | `remains_blocked` |

If the fresh tuple set differs, the binding is stale and validation must fail closed. `preserve_legacy` does not change the producer denominator or convert `recipe_only` to zero.

## Fresh-census decision surface

The focused validator materializes the raw tuples and hashes. The inspected current inputs indicate:

- capability/QG: 86 legacy tuples and 86 QG context tuples, but three identities exist only on each side; count equality is not identity parity;
- Recipe/QG Recipe: 794 legacy tuples, 791 complete QG tuples, and three `qg_decided_no` legacy-only tuples;
- runtime stable `recipe_id` plumbing is a common prerequisite and is not an `identity_unavailable` taxonomy bucket;
- current generated/runtime/canonical top-document mutation remains zero on the blocked path.

These observations select the plan's Gate 3 `BLOCKED` path. They do not approve Change 2 or later policy-dependent current implementation, mixed QG + legacy Recipe presentation, generated installation, or canonical promotion.

## Decision effects

- Required validation on this path: `V1` only.
- `V2`–`V7`, adaptive PZ UI/manual validation, package projection, and Layer 4 generated installation are `not_applicable(no_subject)` under the selected blocked path.
- Allowed output is limited to census/taxonomy, this packet, an isolated off-live stable-ID candidate, no-current-mutation evidence, and an evidence-bounded staging closeout.
- A future full implementation requires external adoption that makes both `capability_only == 0` and `recipe_only == 0`, followed by a fresh structured census.
