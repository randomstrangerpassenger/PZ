# Iris Layer 4 adaptive interaction presentation owner decision packet

Date: 2026-08-21

Status: `OWNER_APPROVED / IMPLEMENTATION INPUT`

This is an additive successor to the historical, ineligible packet at
`docs/iris_layer4_adaptive_interaction_density_presentation_decision_packet.md`.
It does not rewrite or reactivate that packet, the historical blocked closeout,
or the historical staging seal.

## Approval source and execution identity

The explicit approval source is the repository owner's current user message,
supplied in the attached request `pasted-text.txt`. The request expressly
ratifies `L4-RAT-01` through `L4-RAT-07`, declares `L4-RAT-08` not applicable,
approves the three exact QG-only public Layer 4 relations, and authorizes the
policy-dependent implementation scope described below.

- approval source path outside the repository:
  `C:/Users/MW/.codex/attachments/7de3c3fa-5df3-434b-9fd1-1da500359ca0/pasted-text.txt`
- approval source SHA-256:
  `66a65dabef10bf9839e9d94871e092c33bf4a900fea88d0db0f905997e8ff9ce`
- execution base commit:
  `bfdee1c29f82181e15b5924c750e6d44acf41fcc`
- execution base tree:
  `aee455bd36881e1167d454d470300a4f67fa3cf4`
- Layer 4 plan SHA-256:
  `f38d8bf976ec3b8b3d219e2c5e73090a0566a6113b2d658366f26dbdea116580`
- Gate 3 Walkthrough SHA-256:
  `a96a3a6e453d8df0f7549450020dd33b58f2f70cb748038c2bee369f88b376af`

This approval is valid only for that exact base commit/tree and those exact
plan and Gate 3 Walkthrough byte identities. It is not automatically inherited
by another plan, commit, tree, or source-migration subject.

## Approved presentation policy

| ID | Exact owner disposition |
|---|---|
| `L4-RAT-01` | Use four states: `0`, `1`, `2~8`, and `9+`; `small_max=8`, `dense_min=9`. |
| `L4-RAT-02` | Browser owns a private runtime presentation projection. No new semantic authority or separately sealed presentation-policy artifact is created, and QG fact authority does not move into UI. |
| `L4-RAT-03` | Group only by Source. Recipe and Right-click are peer Source sections. No importance, recommendation, representativeness, or semantic grouping is permitted. |
| `L4-RAT-04` | Fixed display order is Recipe then Right-click. This has no priority or importance meaning. |
| `L4-RAT-05` | Preserve QG line order within each Source. The union of compact/full access must preserve every stable identity without representative-only display or arbitrary omission. |
| `L4-RAT-06` | `9+` starts compact, provides literal local search, and keeps the existing single outer scroll. Requirements for `1~8` are immediately accessible; dense requirements may fold per row. Search/fold state must not destroy stable identity. |
| `L4-RAT-07` | Base order is locale-independent. Tooltip is regression-only; no new four-line or Layer 3 consumption is implemented. Disposable package projection is included in full implementation validation. |
| `L4-RAT-08` | `not_applicable`: the fresh subject has no `qg_absent` or `qg_decided_no` legacy-only Recipe tuple. Historical `preserve_legacy` is not reused. |

## Gate 3 binding and newly approved public information

The fresh Gate 3 source-migration result bound to this packet is:

- `capability_only=0`
- `recipe_only=0`
- `qg_only=3`
- `contract_errors=[]`
- `L4-RAT-08=not_applicable`

The owner approves these three QG-only relations as new public Layer 4
information after the legacy fallback cutover:

1. `Base.BallPeenHammer / uc.action.construction`
2. `Base.GardenSaw / uc.action.wood_cutting`
3. `Base.HammerStone / uc.action.construction`

They are retained from QG evidence and are not deleted for count parity or
copied backward into the legacy capability source.

The already approved Gate 3 fact corrections remain separate from adaptive
presentation policy: remove the three false `can_scrap_moveables` capabilities
for `Base.UnusableMetal`, `Base.UnusableWood`, and `Base.WeldingMask`; preserve
the PASS/`role=keep` `remove_battery` relations for `Base.HandTorch`,
`Base.Rubberducky2`, and `Base.Torch`.

## Authorized implementation scope

This packet authorizes Gate 4 and later policy-dependent candidate work under
the existing plan: one status-bearing lookup, lossless private Browser
projection, legacy presentation fallback cutover, `0 / 1 / 2~8 / 9+`
presentation, peer Source counts and sections, compact/full state, literal
local search, row-local dense requirement folding, verified-empty/fault
separation, stale-state invalidation, Recipe navigation/identity preservation,
KO/EN presentation keys, focused tests, standalone/mock Lua acceptance, and
disposable package projection.

It also authorizes task-owned commits required to bind and validate the
implementation. It does not authorize push or external deployment.

## Non-authorized and separate-gate scope

This packet does not authorize modification of the historical packet,
historical blocked closeout, or historical staging seal; Layer 3/DVF changes;
Tooltip four-line implementation; Item-Page Information Sufficiency
reassessment; new QG evidence/fact decisions; ranking, recommendation,
comparison, representative selection, semantic grouping, or raw external-text
inference; direct installation of a generated Layer 4 candidate without the
plan's complete-generation/stateless-validation/safe-install contract; reuse of
the DVF 3.3 installer as Layer 4 authority; early canonical edits to
`DECISIONS.md`, `ARCHITECTURE.md`, or `ROADMAP.md`; substitution of self-tests
for independent review; Publish PASS; release, Workshop, or deployment
readiness.

If Layer 4 generated artifacts must change and no separately authorized Layer
4 generation/install contract exists, generated-dependent scope is deferred or
partial exactly as the plan requires. No candidate is copied into current
runtime/package paths by inference.
